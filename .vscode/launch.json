{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "launch",
			"name": "Run with tsx",
			"skipFiles": ["<node_internals>/**"],
			"runtimeExecutable": "tsx",
			"program": "${file}",
			"args": ["--no-cache"],
			"envFile": "${workspaceFolder}/.env"
		}
	]
}
